---
# LM Studio role defaults

# Installation directory
lmstudio_install_dir: "{{ ansible_env.HOME }}/.local/share/lmstudio"
# Note: Version is now detected dynamically via get_latest_version.py script.
# Hardcoded versions in this file are no longer used by default.
