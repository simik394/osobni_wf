---
- name: Setup Local Environment
  hosts: local
  become: false
  roles:
    - role: julia
    - role: swiprolog
    - role: obs-studio
      # vars:
      #   swiprolog_enabled: false  # Uncomment to skip SWI-Prolog installation
